<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LibraryKotlin"
        tools:targetApi="31">
        <activity
            android:name=".activities.AddBookActivity"
            android:label="Add book"
            android:exported="false" />
        <activity
            android:name=".activities.ReadLaterBooksActivity"
            android:label="Read later"
            android:exported="false" />
        <activity
            android:name=".activities.CurrentlyReadingBooksActivity"
            android:label="Currently reading"
            android:exported="false" />
        <activity
            android:name=".activities.AlreadyReadBooksActivity"
            android:label="Already read"
            android:exported="false" />
        <activity
            android:name=".activities.FavouriteBooksActivity"
            android:label="Favourites"
            android:exported="false" />
        <activity
            android:name=".activities.BookActivity"
            android:exported="false" />
        <activity
            android:name=".activities.AllBooksActivity"
            android:label="All books"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>